{%- doc -%}
  Renders the <img> element using provided image object

  @param {object} image - image object
  @param {number} [height] - custom image height
  @param {string} [class] - additional classes
  @param {string} [text_fallback] - text to display if image is blank
  @param {boolean} [unset_image_tag] - if true, ignores the image focal point
  @param {string} [style] - additional styles
  @param {string} [alt_text_fallback] - Optional text to display if image alt text is blank

  @example
  {% render 'image', image: product.featured_image, height: 300, class: 'product-image' %}
{%- enddoc -%}
{% if image != blank %}
  {% assign image_width = image.width %}
  {% assign image_height = image.height %}
  {% assign image_width_2x = image_width | times: 2 %}
  {% assign image_height_2x = image_height | times: 2 %}
  {% assign image_width_3x = image_width | times: 3 %}
  {% assign image_height_3x = image_height | times: 3 %}
  {% assign image_alt_text = image.alt | default: alt_text_fallback | default: '' %}
  {% assign image_loading = 'lazy' %}

  {% capture image_srcset -%}
    {{ image | image_url: width: image_width, height: image_height, format: 'webp' }} 1x, {{ image | image_url: width: image_width_2x, height: image_height_2x, format: 'webp' }} 2x, {{ image | image_url: width: image_width_3x, height: image_height_3x, format: 'webp' }} 3x
  {%- endcapture %}

  {% assign style_value = style | default: '' %}

  {% if unset_image_tag %}
    {% assign style_value = style_value | append: 'object-position: inherit;' %}
  {% endif %}

  {{
    image
    | image_url: width: image_width, height: image_height, format: 'webp'
    | image_tag: class: class, srcset: image_srcset, style: style_value, alt: image_alt_text, loading: image_loading, width: image_width, height: image_height
  }}
{% elsif text_fallback %}
  {{ text_fallback }}
{% endif %}
